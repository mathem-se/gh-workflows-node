on:
  workflow_call:
    secrets:
      NODE_AUTH_TOKEN:
        required: true

jobs:
  Test:
    uses: mathem-se/gh-workflows-node/.github/workflows/step-test-pull-request.yml@develop
    secrets:
      NODE_AUTH_TOKEN: ${{ secrets.NODE_AUTH_TOKEN }}
